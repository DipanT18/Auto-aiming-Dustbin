# Camera calibration data
# Generated by vision.calibrator.Calibrator.save()
#
# This file is a placeholder. Run the calibration procedure described in
# docs/CAMERA_CALIBRATION.md and save results here.
#
# Fields populated after calibration:
#   rms           — reprojection error (pixels); < 1.0 is good
#   camera_matrix — 3×3 intrinsics [[fx,0,cx],[0,fy,cy],[0,0,1]]
#   dist_coeffs   — radial/tangential distortion [k1,k2,p1,p2,k3]
#   image_size    — [width, height] in pixels
#   px_per_cm     — pixels per centimetre (derived from focal length)
#   board_cols    — checkerboard inner corners (X)
#   board_rows    — checkerboard inner corners (Y)
#   square_size_cm — physical square size used during calibration

rms: null
camera_matrix: null
dist_coeffs: null
image_size: [640, 480]
px_per_cm: 32.0
board_cols: 9
board_rows: 6
square_size_cm: 2.5
